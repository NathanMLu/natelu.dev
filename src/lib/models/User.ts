// export interface User {
//     name?: string;
//     points: number;
//
//     // TODO: May need to refactor so user doesn't lose their identity when cookies are cleared
//     sessionId: string;
// }